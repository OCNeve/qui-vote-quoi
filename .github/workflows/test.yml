name: test
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this is a test from bash"
      - run: python3 -c "import sys;print(f'this is a test from python{sys.version}')"